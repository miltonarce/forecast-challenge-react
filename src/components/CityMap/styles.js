import styled from "styled-components";

export const Container = styled.div`
  background: white;
  -webkit-box-shadow: 3px 4px 16px -9px rgba(0, 0, 0, 0.64);
  -moz-box-shadow: 3px 4px 16px -9px rgba(0, 0, 0, 0.64);
  box-shadow: 3px 4px 16px -9px rgba(0, 0, 0, 0.64);
  width: 18rem;
  height: 14rem;
  border-radius: 8px;
  position: absolute;
  top: 4rem;
  display: flex;
  align-items: center;
  justify-content: center;
`;
